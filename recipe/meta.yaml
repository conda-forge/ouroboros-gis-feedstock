{% set name = "ouroboros-gis" %}
{% set version = "1.0.6" %}
{% set python_min = "3.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/ouroboros_gis-{{ version }}.tar.gz
  sha256: 563c407871d16763d5f932b07ba6f4da794a7ec36571bbfcfb037ca45f81ca04

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools
    - pip
  run:
    - python >={{ python_min }}
    - gdal >=3.8
    - geojson
    - geopandas
    - libsqlite >=3.37
    - sqlite >=3.37

test:
  imports:
    - ouroboros
  commands:
    - pip check
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://github.com/corbel-spatial/ouroboros
  summary: Towards more pythonic use of GeoDatabases
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - p-vdp
